/// oIntraTeleportTrigger — Create

// Destination ID to teleport to
target_id = "";

// Invisible in-game
visible = false;

// Debug rectangle
debug_draw = false;

// Prevent repeated triggering
armed = true;

// Optional: only trigger player
enabled = true;